# Gripper render meshes

Authoritative source supplied by the user:

`/home/cenxi/Downloads/osmo定位.SLDASM.zip`

Imported from `osmo定位.SLDASM/meshes/` without geometric modification. The assembly
bakes the physical Osmo carrier/camera geometry into `base_link.STL`; joint origins remain
those declared by the supplied URDF. These meshes affect display only and never alter the
Tag2-reference training arrays.
