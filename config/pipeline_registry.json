{
  "schema_version": "pipeline-registry/1.0",
  "active": [
    {
      "command": "umi inspect <manifest>",
      "purpose": "verify every input and revision hash"
    },
    {
      "command": "umi process <manifest>",
      "purpose": "run or reuse the manifest-locked processing outputs"
    },
    {
      "command": "umi review <manifest>",
      "purpose": "build an immutable timeline/video/renderer bundle"
    },
    {
      "command": "umi review <manifest> --publish",
      "purpose": "publish the immutable bundle to the review platform"
    },
    {
      "command": "umi verify",
      "purpose": "run every accepted successor baseline"
    },
    {
      "command": "umi progress <pipeline_status.json>",
      "purpose": "serve a live stage, host, progress, artifact, and error dashboard"
    },
    {
      "command": "umi devices ui",
      "purpose": "open the local visual fleet manager with scan, register, and assignment buttons"
    },
    {
      "command": "umi devices scan",
      "purpose": "discover every connected X5 serial and firmware through CameraSDK"
    },
    {
      "command": "umi devices register",
      "purpose": "merge discovered X5 devices into the persistent fleet inventory"
    },
    {
      "command": "umi devices assign <serial> --role <role> --base-tag-id <id>",
      "purpose": "bind a registered camera to a physical gripper role once"
    },
    {
      "command": "umi devices sync",
      "purpose": "upload the persistent fleet inventory to the LAN server"
    },
    {
      "command": "umi devices list",
      "purpose": "show the persistent fleet inventory without reconnecting devices"
    }
  ],
  "legacy": [
    {
      "group": "direct-processing-scripts",
      "status": "internal implementation; do not choose manually for registered captures",
      "files": [
        "src/osmo360/visualization/render_gripper_force_angle_demo.py",
        "src/osmo360/visualization/render_single_gripper_webgl_demo.py",
        "tools/osmo_360_offline.py",
        "tools/insta360_media_stitch.py"
      ]
    },
    {
      "group": "historical-experiments",
      "status": "diagnostic or superseded; require an explicit historical reproduction task",
      "patterns": [
        "tools/estimate_*",
        "tools/run_*experiment*",
        "tools/render_*comparison*",
        "tools/dual_gripper_calibrator*"
      ]
    },
    {
      "group": "legacy-dual-gripper-tools",
      "status": "historical implementation; not part of the active baseline verifier",
      "files": [
        "src/osmo360/visualization/render_fused_world_audit.py",
        "src/osmo360/localization/fuse_asymmetric_gripper_world_pose.py"
      ]
    }
  ]
}
